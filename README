tirc is a server that can be connected to via telnet and provides an 
extremely rudimentary chat room. The 'irc' in the name is purely optimistic 
as the server makes no attempt to implement the IRC protocol.
